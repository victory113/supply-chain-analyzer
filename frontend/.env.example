# Copy to .env.local for local overrides.
#
# Leave unset to use relative /api URLs, which the Vite dev proxy (local) and
# the Netlify redirect (production) forward to the backend. Only set this when
# pointing at a backend on a different origin without a proxy in front.
# VITE_API_BASE_URL=http://localhost:8000
