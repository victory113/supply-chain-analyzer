-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
httpx==0.28.1
# Backs both the test suite and the no-Postgres local dev mode. Deliberately
# absent from requirements.txt so the production image cannot fall back to it.
aiosqlite==0.20.0
ruff==0.8.4
mypy==1.14.0
types-redis==4.6.0.20241004
